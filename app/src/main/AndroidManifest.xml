<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.saurabh.wings2017">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CivilHome"
            android:label="@string/title_activity_civil_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".signIn"
            android:label="@string/title_activity_civil_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BrainHome"
            android:label="@string/title_activity_brain_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".GenericEventHome" />
        <activity android:name=".Cart" />
        <activity android:name=".Checkout" />
        <activity android:name=".confirmOrder" />
        <activity android:name=".tickets" />
        <activity android:name=".Profile" />
        <activity android:name=".Details" />
        <activity android:name=".Team" />
        <activity android:name=".Developer" />
        <activity android:name=".Brain" />
        <activity android:name=".Code" />
        <activity android:name=".Mini" />
        <activity android:name=".Talent" />
        <activity android:name=".Robotics"></activity>
    </application>

</manifest>